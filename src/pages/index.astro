---
import Layout from '../layouts/Layout.astro';
import { getCollection } from 'astro:content';
import { getRecentChanges, formatChangelogDate, getChangeTypeEmoji } from '../utils/changelog';

// Query all devices and projects
const allDevices = await getCollection('devices');
const allProjects = await getCollection('projects');

// Compute display status for each device based on project usage
const devicesWithComputedStatus = allDevices.map(device => {
  const projectsUsingDevice = allProjects.filter(project => {
    const hasDevices = project.data.devices && Array.isArray(project.data.devices);
    if (!hasDevices) return false;
    
    const deviceSlugLower = device.slug.toLowerCase();
    return project.data.devices.some(d => d.toLowerCase() === deviceSlugLower);
  });

  let displayStatus = device.data.status;
  
  // Only override if device is currently marked as 'ready'
  if (projectsUsingDevice.length > 0 && device.data.status === 'ready') {
    const hasDeployedProject = projectsUsingDevice.some(p => p.data.status === 'completed');
    const hasInProgressProject = projectsUsingDevice.some(p => p.data.status === 'in-progress');
    
    if (hasDeployedProject) {
      displayStatus = 'deployed';
    } else if (hasInProgressProject) {
      displayStatus = 'testing';
    }
  }

  return {
    ...device,
    displayStatus
  };
});

// Calculate stats based on computed status
const deviceStats = {
  total: devicesWithComputedStatus.length,
  ready: devicesWithComputedStatus.filter(d => d.displayStatus === 'ready').length,
  testing: devicesWithComputedStatus.filter(d => d.displayStatus === 'testing').length,
  deployed: devicesWithComputedStatus.filter(d => d.displayStatus === 'deployed').length,
  retired: devicesWithComputedStatus.filter(d => d.displayStatus === 'retired').length,
  pending: devicesWithComputedStatus.filter(d => d.displayStatus === 'pending').length,
  unsupported: devicesWithComputedStatus.filter(d => d.displayStatus === 'unsupported').length,
};

const projectStats = {
  total: allProjects.length,
  ideas: allProjects.filter(p => p.data.status === 'idea').length,
  inProgress: allProjects.filter(p => p.data.status === 'in-progress').length,
  completed: allProjects.filter(p => p.data.status === 'completed').length,
  abandoned: allProjects.filter(p => p.data.status === 'abandoned').length,
};

// Query all notes
const allNotes = await getCollection('notes');
const noteStats = {
  total: allNotes.length,
};

// Get ready or testing devices for random picker (using computed status)
const availableDevices = devicesWithComputedStatus.filter(d => 
  d.displayStatus === 'ready' || d.displayStatus === 'testing'
);

// Pick random one
const randomDevice = availableDevices.length > 0 
  ? availableDevices[Math.floor(Math.random() * availableDevices.length)]
  : null;

// Random hero descriptions (after the first one)
const heroDescriptions = [
  "This is my personal documentation as I turn all my junk into smart home devices.",
  "As they say - one man's garbage is another man's treasure, just in this case I'm that same man! 😂 Maybe the world just wasn't ready for me all these years!",
];

const randomHeroDescription = heroDescriptions[Math.floor(Math.random() * heroDescriptions.length)];

// Get recent changes for "What's New" section
const recentChanges = await getRecentChanges(7);
---

<Layout title="Home">
  <div class="text-center py-12 bg-gradient-to-br from-blue-600 to-blue-700 dark:from-slate-800 dark:to-slate-900 text-white rounded-lg mb-12 dark:border dark:border-slate-700">
    <h1 class="m-0 mb-4 text-4xl text-white">My ESPHome Workshop</h1>
    <p class="text-lg max-w-2xl mx-auto mb-4 opacity-95 dark:opacity-90">
      After 15+ years collecting dev boards, sensors, and random gadgets, 
      it's finally time to build something useful.
    </p>
    <p class="text-lg max-w-2xl mx-auto mb-0 opacity-95 dark:opacity-90">{randomHeroDescription}</p>
  </div>

  <div class="grid grid-cols-2 gap-6 mb-8">
    <a href="/devices" class="block bg-white dark:bg-slate-800 border border-gray-200 dark:border-slate-700 rounded-lg p-5 no-underline text-gray-800 dark:text-slate-100 transition-all duration-200 shadow-sm hover:-translate-y-0.5 hover:shadow-xl hover:border-blue-600 dark:hover:border-blue-400">
      <div class="flex justify-between items-center mb-2">
        <h2 class="m-0 mb-2 text-xl text-blue-600 dark:text-blue-400">📟 Devices</h2>
        <div class="text-5xl font-bold text-blue-600 dark:text-blue-400">{deviceStats.total}</div>
      </div>
      <p class="m-0 mb-4 text-gray-500 dark:text-slate-400 text-sm">Browse physical hardware - boards, sensors, displays, and more.</p>
      <div class="flex flex-col gap-2 text-sm">
        <span class="px-2 py-1 rounded bg-emerald-100 dark:bg-emerald-900 text-emerald-800 dark:text-emerald-200">✅ {deviceStats.deployed} deployed</span>
        <span class="px-2 py-1 rounded bg-amber-100 dark:bg-amber-900 text-amber-800 dark:text-amber-200">🧪 {deviceStats.testing} testing</span>
        <span class="px-2 py-1 rounded bg-gray-100 dark:bg-slate-700 text-gray-600 dark:text-slate-300">📦 {deviceStats.ready} ready</span>
        <span class="px-2 py-1 rounded bg-amber-100 dark:bg-amber-900 text-amber-800 dark:text-amber-200">🔦 {deviceStats.pending} pending</span>
        <span class="px-2 py-1 rounded bg-red-100 dark:bg-red-900 text-red-800 dark:text-red-200">❕ {deviceStats.unsupported} unsupported</span>
        {deviceStats.retired > 0 && (
          <span class="px-2 py-1 rounded bg-red-100 dark:bg-red-900 text-red-800 dark:text-red-200">⏸️ {deviceStats.retired} retired</span>
        )}
      </div>
    </a>

    <a href="/projects" class="block bg-white dark:bg-slate-800 border border-gray-200 dark:border-slate-700 rounded-lg p-5 no-underline text-gray-800 dark:text-slate-100 transition-all duration-200 shadow-sm hover:-translate-y-0.5 hover:shadow-xl hover:border-blue-600 dark:hover:border-blue-400">
      <div class="flex justify-between items-center mb-2">
        <h2 class="m-0 mb-2 text-xl text-blue-600 dark:text-blue-400">💡 Projects</h2>
        <div class="text-5xl font-bold text-blue-600 dark:text-blue-400">{projectStats.total}</div>
      </div>
      <p class="m-0 mb-4 text-gray-500 dark:text-slate-400 text-sm">Complete projects with wiring and configuration.</p>
      <div class="flex flex-col gap-2 text-sm">
        <span class="px-2 py-1 rounded bg-emerald-100 dark:bg-emerald-900 text-emerald-800 dark:text-emerald-200">✅ {projectStats.completed} completed</span>
        <span class="px-2 py-1 rounded bg-orange-200 dark:bg-orange-900 text-orange-900 dark:text-orange-200">🛠️ {projectStats.inProgress} in progress</span>
        <span class="px-2 py-1 rounded bg-blue-100 dark:bg-blue-900 text-blue-800 dark:text-blue-200">💭 {projectStats.ideas} ideas</span>
        {projectStats.abandoned > 0 && (
          <span class="px-2 py-1 rounded bg-gray-100 dark:bg-slate-700 text-gray-600 dark:text-slate-300">⏸️ {projectStats.abandoned} abandoned</span>
        )}
      </div>
    </a>
  </div>

  {randomDevice && (
    <div class="mb-8 p-5 bg-gradient-to-br from-blue-100 to-blue-50 dark:from-slate-800 dark:to-slate-900 rounded-lg border-2 border-blue-600 dark:border-blue-500">
      <h2 class="m-0 mb-4 text-xl text-blue-900 dark:text-blue-300">🎲 Random Device to Try Next</h2>
      <div class="bg-white dark:bg-slate-800 p-4 rounded-lg grid grid-cols-[auto_1fr] gap-6 items-center border dark:border-slate-700">
        <div>
          <h3 class="m-0 text-gray-800 dark:text-slate-100 text-lg whitespace-nowrap">{randomDevice.data.title}</h3>
        </div>
        <div class="flex flex-col gap-2">
          <p class="text-gray-500 dark:text-slate-400 m-0 text-sm">{randomDevice.data.description}</p>
          <div class="flex gap-2 flex-wrap">
            <span class="inline-block text-xs px-2 py-0.5 rounded bg-blue-100 dark:bg-blue-900 text-blue-800 dark:text-blue-200">
              {randomDevice.data.category}
            </span>
            {randomDevice.data.connectionTypes.map(type => (
              <span class="inline-block text-xs px-2 py-0.5 rounded bg-gray-100 dark:bg-slate-700 text-gray-700 dark:text-slate-300">{type}</span>
            ))}
          </div>
          <a href={`/devices/${randomDevice.slug}`} 
             class="inline-block w-fit px-4 py-2 bg-blue-600 dark:bg-blue-500 text-white no-underline rounded-lg font-medium text-sm transition-all duration-200 hover:bg-blue-700 dark:hover:bg-blue-400 hover:-translate-y-0.5">
            Let's Use This! →
          </a>
        </div>
      </div>
    </div>
  )}

  {recentChanges.length > 0 && (
    <div class="mb-8 p-5 bg-white dark:bg-slate-800 rounded-lg border border-gray-200 dark:border-slate-700 shadow-sm">
      <h2 class="m-0 mb-4 text-xl text-gray-800 dark:text-slate-100">✨ What's New</h2>
      <div class="flex flex-col gap-3">
        {recentChanges.map(change => (
          <div class="flex items-baseline gap-2 text-sm px-2 py-2 rounded transition-colors duration-200 hover:bg-gray-100 dark:hover:bg-slate-700">
            <span class="shrink-0">{getChangeTypeEmoji(change.type)}</span>
            <span class="shrink-0 font-medium capitalize text-gray-700 dark:text-slate-300">{change.type}:</span>
            <a href={`/${change.contentType}s/${change.slug}`} 
               class="text-blue-600 dark:text-blue-400 no-underline font-medium transition-colors duration-200 hover:text-blue-700 dark:hover:text-blue-300 hover:underline">
              {change.title}
            </a>
            <span class="text-gray-500 dark:text-slate-400">- {change.description}</span>
            <span class="text-gray-500 dark:text-slate-400 text-[0.85rem] ml-auto shrink-0">
              ({formatChangelogDate(change.date)})
            </span>
          </div>
        ))}
      </div>
    </div>
  )}

  <div class="grid grid-cols-2 gap-5 mb-12">
    <a href="/components" class="block bg-white dark:bg-slate-800 border border-gray-200 dark:border-slate-700 rounded-lg p-5 no-underline text-gray-800 dark:text-slate-100 transition-all duration-200 shadow-sm hover:-translate-y-0.5 hover:shadow-xl hover:border-blue-600 dark:hover:border-blue-400">
      <h2 class="m-0 mb-2 text-xl text-blue-600 dark:text-blue-400">🔧 Components</h2>
      <p class="m-0 text-gray-500 dark:text-slate-400 text-sm">Explore ESPHome software components and platforms.</p>
    </a>
    <a href="/notes" class="block bg-white dark:bg-slate-800 border border-gray-200 dark:border-slate-700 rounded-lg p-5 no-underline text-gray-800 dark:text-slate-100 transition-all duration-200 shadow-sm hover:-translate-y-0.5 hover:shadow-xl hover:border-blue-600 dark:hover:border-blue-400">
      <h2 class="m-0 mb-2 text-xl text-blue-600 dark:text-blue-400">📓 Notes</h2>
      <p class="m-0 text-gray-500 dark:text-slate-400 text-sm">Setup guides, networking tricks, and lessons learned along the way.</p>
    </a>
  </div>
</Layout>
