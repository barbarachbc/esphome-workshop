---
import { getCollection } from 'astro:content';
import Layout from '../../layouts/Layout.astro';
import ComponentCard from '../../components/ComponentCard.astro';

const components = await getCollection('components');
const categories = [...new Set(components.map(c => c.data.category))];
---

<Layout title="Components">
  <h1 class="text-4xl font-semibold mt-0 mb-6">Components</h1>
  <p class="text-gray-500 text-lg mb-8">ESPHome software components and platforms</p>

  <div class="mb-8 p-4 bg-gray-100 rounded-lg">
    <label class="flex items-center gap-2">
      <span class="font-medium">Filter by category:</span>
      <select id="categoryFilter" class="px-3 py-2 border border-gray-200 rounded text-base">
        <option value="">All Categories</option>
        {categories.map(cat => (
          <option value={cat}>{cat}</option>
        ))}
      </select>
    </label>
  </div>

  <div class="grid grid-cols-[repeat(auto-fill,minmax(300px,1fr))] gap-6" id="componentGrid">
    {components.map(component => (
      <div data-category={component.data.category}>
        <ComponentCard component={component} />
      </div>
    ))}
  </div>

  <script>
    const filter = document.getElementById('categoryFilter') as HTMLSelectElement;
    const grid = document.getElementById('componentGrid');
    
    filter?.addEventListener('change', () => {
      const value = filter.value;
      const cards = grid?.querySelectorAll('[data-category]');
      
      cards?.forEach(card => {
        const category = card.getAttribute('data-category');
        if (!value || category === value) {
          (card as HTMLElement).style.display = 'block';
        } else {
          (card as HTMLElement).style.display = 'none';
        }
      });
    });
  </script>
</Layout>
